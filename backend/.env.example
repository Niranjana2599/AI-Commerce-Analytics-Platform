# Copy to .env and adjust paths only when data/models are stored outside the repository.
APP_ENV=production
CORS_ORIGINS=*
# MASTER_DATA_PATH=/app/data/processed/master_df.parquet
# MODELS_DIR=/app/models
